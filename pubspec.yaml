name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.0.19
  intl: ^0.20.2
  mqtt_client: ^10.11.1
  provider: ^6.0.5   # ⚡ AJOUTÉ
  flutter_riverpod: ^3.0.3
  flutter_localizations:
    sdk: flutter
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
